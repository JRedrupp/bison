name: Nightly

on:
  schedule:
    - cron: '0 3 * * *'  # daily at 03:00 UTC
  workflow_dispatch:      # allow manual triggers
  pull_request:           # run on every PR to catch nightly breakage early
  push:
    branches: [main]      # run on every merge to main

jobs:
  test-nightly:
    name: test (nightly)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Switch to Mojo nightly channel
        run: |
          sed -i \
            's|https://conda.modular.com/max"|https://conda.modular.com/max-nightly"|g' \
            pixi.toml

      - uses: prefix-dev/setup-pixi@v0.9.4
        with:
          locked: false

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Normalize formatting to nightly mblack
        # The nightly mblack version may format differently from the locked
        # version. Re-format before linting so the format hook sees clean files.
        run: pixi run fmt

      - name: Run pre-commit checks
        run: pixi run lint

      - name: Generate version file
        run: pixi run gen-version

      - name: Run tests
        run: pixi run test
